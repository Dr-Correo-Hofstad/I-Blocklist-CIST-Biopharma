# I-Blocklist-CIST-Biopharma
This application functions as an automated daemon that periodically fetches your premium iBlocklist feeds, unzips the archives, parses the IP ranges, cross-references them against your server’s active logs or network connections, reverse-geocodes matching threats via Google Maps, and emails the structured intelligence reports every 2 days.
